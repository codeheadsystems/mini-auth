package com.codeheadsystems.miniconsole;

/**
 * Placeholder for mini-console, a future optional unified admin UI over the family.
 *
 * <p><b>Roadmap only.</b> This module deliberately contains no UI or server logic. It reserves the
 * name and package so the umbrella build and the direction doc can refer to it as a real,
 * compiling module before any code exists. See {@code docs/DIRECTION.md}.
 */
public final class MiniConsole {

  /** Marks this module as a not-yet-implemented roadmap track. */
  public static final boolean IMPLEMENTED = false;

  private MiniConsole() {
  }
}
